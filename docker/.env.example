# Copy this file to `.env` in this directory (docker compose reads its
# .env from the directory containing the compose file it's given) before
# running `docker compose -f docker/docker-compose.yml up` (or `make
# up` from the repo root, which points at this same file).
#
# Generate a real secret with, e.g.: openssl rand -hex 32
JWT_SECRET=
